


write view proc 페이지에 검색관련으로 f_name등 여러가지가 있는 이유는 검색 부분에서 검색해서 수정이나 리스트로 돌아올때 검색에 해당 값이 그대로 남아있게 하기 위해서 하는것

고정적인 변하지 않는값은 굳이 테이블에 담을 이유가 없다. 3개의 테이블을 select 해서 화면에 뿌려진다 굳이 1개의 테이블에다가 insert할 필요가 없다 만약에 insert를 하게 되면
나중에 값이 변경됐을때 수정된 테이블 하나와 추가로 수정된 테이블 1개를 또 변경해야 되는데 값이 많아지면 하나 하나 다 수정해야 되므로

member 테이블과 setup 테이블 proof 테이블을 다 select해서 한 화면에 다 뿌려준다

그리고 지금은 초보라서 select로 한개 한개 한개씩 다 불러오는데 나중에는 join문을 써서 쿼리문을 날려서 한다(join문은 개별적으로 공부할 것).

